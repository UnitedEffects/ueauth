extends ../uelayout

block content
    script(type="text/javascript").
        const gMfa = '#{params.globalMfa}';
        const pwl = '#{params.passwordless}';
        console.info(gMfa);
        console.info(pwl);
    body.bg.flex
        if(align === 'middle')
            .panel.middle-panel.width-auto
                include primary
        else
            .panel
                include primary
