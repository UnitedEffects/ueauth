doctype html

head
    meta(charset='UTF-8')
    title=title
    link(rel='stylesheet' type='text/css' href='https://cdn.jsdelivr.net/npm/swagger-ui-dist@3/swagger-ui.css')
    if favicon
        link(rel="icon" type="image/png" href=favicon sizes="100x100")
    else
        link(rel="icon" type="image/png" href="/favicon.png" sizes="100x100")
    style.
        html
        {
            box-sizing: border-box;
            overflow: -moz-scrollbars-vertical;
            overflow-y: scroll;
        }
        *,
        *:before,
        *:after
        {
            box-sizing: inherit;
        }
        body
        {
            margin:0;
            background: #fafafa;
        }
#swagger-ui
script(src='https://cdn.jsdelivr.net/npm/swagger-ui-dist@3/swagger-ui-bundle.js')
script(src='https://cdn.jsdelivr.net/npm/swagger-ui-dist@3/swagger-ui-standalone-preset.js')
script.
    window.onload = function() {
        const group = '#{group}';
        window.ui = SwaggerUIBundle({
            url: (!group) ? "/swagger.json" : `/${group}/swagger.json`,
            dom_id: '#swagger-ui',
            deepLinking: true,
            presets: [
                SwaggerUIBundle.presets.apis,
                SwaggerUIStandalonePreset
            ],
            plugins: [
                SwaggerUIBundle.plugins.DownloadUrl
            ],
            layout: "StandaloneLayout"
        })
    }
